# Telegram Bot API Token (get from BotFather)
# This is a secret and should be kept private.
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Database connection URL.
# For PostgreSQL with asyncpg driver (recommended for this project):
# Example: DATABASE_URL=postgresql+asyncpg://user:password@host:port/database_name
# For SQLite (for simpler local testing, ensure your models and db_utils are compatible if you switch):
# Example: DATABASE_URL=sqlite+aiosqlite:///./your_database_name.db
DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/telegram_bot_db

# Comma-separated list of Telegram User IDs who are authorized as bot administrators.
# No spaces between commas and IDs.
# Example: ADMIN_USER_IDS=12345678,98765432
ADMIN_USER_IDS=123456789

# Optional: Default cryptocurrency wallet address for payments.
# This can be used as a fallback if a specific chat does not have its own wallet configured.
DEFAULT_CRYPTO_WALLET=

# Optional: Default cryptocurrency network (e.g., Ethereum, BSC, Tron, Bitcoin).
# This is used in conjunction with DEFAULT_CRYPTO_WALLET.
DEFAULT_CRYPTO_NETWORK=

# Optional: Number of days before a subscription expires to send a renewal reminder.
# Defaults to 3 if not set.
REMINDER_DAYS_AHEAD=3

# Optional: Logging level for the application.
# Valid values: CRITICAL, ERROR, WARNING, INFO, DEBUG, NOTSET
# Defaults to INFO if not set or invalid.
LOG_LEVEL=INFO
